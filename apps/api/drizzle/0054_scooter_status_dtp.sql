-- Новый статус скутера «ДТП» (попал в аварию). По просьбе заказчика —
-- отдельный статус для аварийных скутеров (помимо «Ремонт» и «Разборка»).
-- ADD VALUE идемпотентен (IF NOT EXISTS), запускается вне транзакции —
-- мигратор применяет каждый statement отдельным sql.unsafe().
ALTER TYPE "scooter_base_status" ADD VALUE IF NOT EXISTS 'dtp';
