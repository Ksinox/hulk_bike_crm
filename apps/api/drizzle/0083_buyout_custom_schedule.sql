-- Выкуп задним числом (заказчик 06.09, п.15).
--
-- Сделку могли оформить раньше, чем завели в CRM: часть уже выплачена,
-- остаток раскидан по датам вручную (25 000 = 10 000 01.02 + 10 000 01.03
-- + 5 000 20.03). Свой график хранится в сделке до подписания; при
-- подписании превращается в строки buyout_schedule, оплаченные — сразу
-- закрытыми с записями о поступлении на нужную дату.
ALTER TABLE "buyout_deals"
  ADD COLUMN IF NOT EXISTS "custom_schedule" jsonb;
