-- Причина удаления аренды в архив («Создано случайно» / тест / дубль / своё).
-- Заказчик: при случайно созданной аренде её можно удалить с пометкой, чтобы
-- в архиве было видно почему связка убрана. Показывается в списке архива.
ALTER TABLE "rentals" ADD COLUMN IF NOT EXISTS "archived_reason" text;
