-- Причина переноса скутера в архив (продан / списан / разбит / дубль / своё).
-- Показывается в списке архива, чтобы было видно почему позиция убрана из парка.
ALTER TABLE "scooters" ADD COLUMN IF NOT EXISTS "archived_reason" text;
