-- Рыночная стоимость скутера (₽) — для пункта договора «стоимость Скутера при
-- утрате» (п. 4.1). Раньше в договор подставлялась ЗАКУПОЧНАЯ (purchase_price);
-- теперь отдельное поле рыночной стоимости. NULL → договор падает обратно на
-- purchase_price (обратная совместимость со старыми скутерами).
ALTER TABLE "scooters" ADD COLUMN IF NOT EXISTS "market_value" integer;
