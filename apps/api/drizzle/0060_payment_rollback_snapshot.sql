-- Снимок пред-состояния аренды на момент действия, создавшего платёж
-- (продление / смена экипировки). Нужен для «отката в день действия»:
-- при откате восстанавливаем аренду из этого снимка. NULL — у платежей,
-- которые аренду не меняли (обычная оплата): их откат = снять флаг «оплачено».
ALTER TABLE "payments" ADD COLUMN "rollback_snapshot" jsonb;
