-- G3 (v0.6) — предзаявка: желаемая дата начала аренды («мягкая бронь»).
-- Клиент указывает, с какого числа хочет взять скутер; при конвертации
-- подставляется в дату выдачи аренды. Скутеры не блокируются — это ориентир.

ALTER TABLE "client_applications"
  ADD COLUMN IF NOT EXISTS "requested_start_date" date;
