/**
 * Канонический текст пользовательского соглашения «Инструктаж при передаче
 * скутера» (предоставлен владельцем, .docx). ЕДИНЫЙ источник истины —
 * используется и в публичной анкете (R2.7, экран принятия), и во вкладке
 * «Документы» в CRM. Текст менять НЕЛЬЗЯ — приведён дословно.
 */
export const RENTAL_AGREEMENT_TITLE = "Инструктаж при передаче скутера";

export const RENTAL_AGREEMENT_TEXT = `Инструктаж при передаче скутера

Запрещено:
— Передача скутера третьим лицам — штраф по договору 2 000 руб.
— Выезд за границу г. Краснодара +25 км; при нарушении подача топлива автоматически отрезается, штраф 2 500 руб.
— Катание более одного человека — штраф 2 000 руб.
— Нарушение пломб (внешние/внутренние наклейки и пломбировочная краска). При нарушении целостности — разбор и диагностика техники.
В случае нарушения предусмотрена материальная ответственность по договору.

Зона ответственности арендатора:
— Контроль индикации температуры двигателя. При загорании — немедленно заглушить и сообщить нам (эвакуируем, дадим подменный). Езда на перегретом двигателе — мат. ответственность по договору.
— Контроль пробега. Под сиденьем стикер с пробегом; при совпадении со спидометром — приехать в парк на замену масла. Нарушение регламента замены масла — ответственность.
— Пластик. Состояние фиксируется круговой подсъёмкой и отправляется в общий чат WhatsApp. Клиент проверяет видео; если что-то не отражено — сам делает фото и присылает в диалог.
— Рекомендуется взять противоугонную цепь и пристёгивать у подъезда за заднее колесо перед камерой.

Оплата:
Производится в день продления договора; чек об оплате — в диалог с менеджером. При нарушении условий оплаты стоимость аренды рассчитывается по суточному тарифу.

ДТП и Дефекты:
— Мелкая потёртость (лак) — 1 000 руб. (полировка)
— Царапины/потёртости/сколы (ЛКП) — 3 700 руб. (покраска)
— Трещина пластика больше 5 см — замена детали
— Трещина пластика менее 5 см — пайка — 1 500 руб.

Просьба относиться к технике бережно, как к своей =)`;

/** Экранирование для безопасной вставки текста в HTML. */
function escapeHtml(s: string): string {
  return s
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;");
}

/**
 * Печатная HTML-версия инструктажа (для предпросмотра/печати/скачивания в
 * Word из вкладки «Документы»). Текст вставляется ДОСЛОВНО (pre-wrap) —
 * меняется только оформление, не содержание. Первая строка — заголовок.
 */
export function rentalAgreementHtmlDoc(): string {
  const [titleLine, ...rest] = RENTAL_AGREEMENT_TEXT.split("\n");
  const body = rest.join("\n").replace(/^\n+/, "");
  return `<!doctype html>
<html lang="ru">
<head>
<meta charset="utf-8" />
<title>${escapeHtml(RENTAL_AGREEMENT_TITLE)}</title>
<style>
  @page { margin: 18mm; }
  * { box-sizing: border-box; }
  body {
    font-family: -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    color: #111827; line-height: 1.5; margin: 0; padding: 24px;
  }
  .doc { max-width: 720px; margin: 0 auto; }
  h1 { font-size: 20px; font-weight: 800; margin: 0 0 16px; }
  pre {
    font-family: inherit; font-size: 14px; white-space: pre-wrap;
    word-wrap: break-word; margin: 0;
  }
</style>
</head>
<body>
  <div class="doc">
    <h1>${escapeHtml(titleLine)}</h1>
    <pre>${escapeHtml(body)}</pre>
  </div>
</body>
</html>`;
}
