# Корректировки заказчика (2026-08-24, до приёмки)

Это не приёмка — заказчик прошёлся по отчёту и дал корректировки. Каждая
правка ниже — в работу; после выполнения обновляем истории на «Развитии».

## 1. Мессенджеры (пункт 3) — переиспользовать открытую вкладку
- [x] Клик по WhatsApp / Telegram / MAX не должен плодить новые вкладки:
      если вкладка мессенджера уже открыта — работать в ней.
- 💬 Технически: браузер не даёт «захватить» вкладку, открытую вручную.
  Что реально работает — именованное окно (`window.open(url, 'hulk_wa')`):
  все последующие клики ИЗ CRM попадают в ту же вкладку. Делаем так.

## 2. «Новая сделка» (пункт 5) — переименовать
- [x] «Рассрочка» → **«Выкуп»** (понятия «рассрочка» в бизнесе нет,
      раздел будет называться «Выкупы»). Остальное («Продажа», «Ремонт»,
      «скоро») — верно.

## 3. Способ оплаты (пункт 8) — переработать UX
- [x] Три РАВНОЗНАЧНЫЕ кнопки в ряд: «Наличные» · «Перевод» · «Разделить».
- [x] Выбрал «Наличные»/«Перевод» → появляется блок ввода суммы.
- [x] Выбрал «Разделить» → кнопки нал/перевод скрываются (плавно),
      появляются два поля: наличными / безналом.
- [x] Никаких «кнопок другого формата» — всё в одном стиле, живая анимация.

## 4. Электро-отметка (пункты 11, 12, 14) — иконки вместо эмодзи
- [x] ЭМОДЗИ УБРАТЬ ВЕЗДЕ. Только иконки (lucide).
- [x] Отметка электро в списке аренд: заметная иконка в начале строки,
      крупнее, чтобы выделялась среди бензиновых (сейчас «е-» теряется).
- [x] Анкета (п. 12): крупные плашки выбора с иконками —
      «скутер + канистра» (бензин) и «скутер + молния» (электро),
      чтобы не читалось как «продают бензин».
- [x] Карточки моделей: иконка электро / иконка канистры вместо текста,
      свой цвет.

## 5. Партнёрская техника — свойство СКУТЕРА, не модели
- [x] Флаг «партнёрская» перенести с модели на конкретный скутер:
      модель может быть одна, а экземпляры — наши и партнёрские.
- [x] Процент инвестора: общий процент по умолчанию + галочка
      «применить ко всей технике».
- [x] У конкретной техники можно задать свой процент.
- [x] При «применить ко всем», если есть техника со своим процентом —
      предупреждение с выбором: применить ко всем / применить ко всем,
      кроме тех, где выставлено вручную.

## 6. Номера и ID (пункт 15)
- [x] Уникальный ID = **6 последних цифр VIN** (не 4 цифры рамы).
- [x] «Место» → **«Номер»** во всех формулировках.
- [x] Формат отображения скутера: **убрать «#03» ВЕЗДЕ по CRM**.
      Вместо — название модели + круглый бейдж с номером (как в арендах):
      «Jog ⑤».
- [x] Убрать плашку «Арендных мест 8 из 8» со страницы «Скутеры» —
      лишняя сущность. Номер выдаётся автоматически (следующий свободный),
      менять/меняться местами можно вручную.

## 7. История техники при уходе из аренды (пункт 16)
- [x] Вместо одного «был в аренде · место №N» — история: сколько аренд,
      сколько дней в аренде, какие номера были у скутера.
- [x] «Место» → «Номер» и здесь.

## 8. Окно добавления техники (пункт 14) — задизайнить
- [x] Выглядит старомодно, переработать.
- [x] Аватарка: показать требования (формат/размер/прозрачный фон),
      возможность кропа, и живое превью карточки скутера сбоку —
      как она будет выглядеть по мере заполнения формы.

## Проверить отдельно
- [x] Скриншоты пунктов 6 и 24 пересняты и переименованы (v2), проверены
      на живом лендинге.

## Сделано вне списка (в тот же день)
- [x] ПРОД: восстановлены штрафы за просрочку — оплаты штрафа за прошлые
      просрочки съедали новый штраф. Введён «эпизод просрочки»
      (см. память `overdue_fine_epoch_fix`). Хотфикс в main, прод-API
      задеплоен, суммы вернулись (171 +1000, 137 +250, 164 +1750).
- [x] ПРОД: в составе долга видно, за сколько дней начислено
      («штраф за 4 дн просрочки» вместо «штраф»).
